#include <stdio.h>

int main()
{
    char s[101];
    int cnt = 0, i = 0;
    // scanf("%s", s);

    while (s[i])
    {
        printf("a");
        // if (s[i] = 'v')
        //     cnt++;
        // else
        //     cnt += 2;
        // i++;
    }
    printf("%d\n", cnt);
    return 0;
}
